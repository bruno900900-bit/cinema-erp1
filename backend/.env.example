# Backend Environment Example

# Core Security
SECRET_KEY=change-this-secret
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Database (choose one)
# Local SQLite fallback (NOT for production)
# DATABASE_URL=sqlite:///./cinema_erp.db
# Temporary ephemeral in container
# DATABASE_URL=sqlite:////tmp/cinema_erp.db
# PostgreSQL example
DATABASE_URL=postgresql://user:password@host:5432/cinema_erp

# Optional: OpenAI (for AI enrichment)
# OPENAI_API_KEY=sk-...

# PDF server-side (Playwright)
# PLAYWRIGHT_ENABLED=1

# Other optional future variables
# REDIS_URL=redis://host:6379/0
# LOG_LEVEL=info
